<script>
  
  function swymCallbackFn(swat) {
     // your API calls go here
     // use swat within this scope, 
     // alternatively you can use window._swat
       
    swat.fetchLists({
      callbackFn: function(lists) {

        console.log("Fetched my lists", lists);
      },
      errorFn: function(xhrObj) {
        // something went wrong
      }
    });
    
    swat.fetchListDetails({lid: "4715d976-7bdb-4d29-b0de-e7f5e95755c4"}, function(listContents){

    }, function(xhrObj) {
      // something went wrong
    });
  }
  if(!window.SwymCallbacks) {
   window.SwymCallbacks = [];
  }
  window.SwymCallbacks.push(swymCallbackFn);
  
  

</script>